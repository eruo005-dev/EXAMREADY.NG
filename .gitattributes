# Force LF on all text files (Windows devs included). Matches .editorconfig.
* text=auto eol=lf

# Binaries — never normalize
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.pdf binary
*.woff binary
*.woff2 binary
*.zip binary
