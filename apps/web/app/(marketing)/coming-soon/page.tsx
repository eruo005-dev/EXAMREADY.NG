import { ComingSoonContent } from './ComingSoonContent';

export const metadata = {
  title: 'Coming soon — exams we\'re building next',
  description:
    'WAEC, NECO, JUPEB, IELTS, SAT, ICAN, Cambridge IGCSE, and 30+ Post-UTME screenings — be first to know when each launches on ExamReady.ng.',
};

export default function ComingSoonPage() {
  return <ComingSoonContent />;
}
